//working out new config muncher - NOM OM NOM ARHGHG

var configObject;

function getYoutubeRSS(configObject) {
	var tempObj = configObject["youtube"];
	var splitURL = configObject["youtube"]["rssURL"].split("{{}}");
	for(var index = 0; i < splitURL.length; i++){

	}
	for(var key in tempObj){
		if(tempObj[key])
	}
}